# Custom Hooks

Contains general-purpose, reusable custom React hooks (e.g., `useAuth`, `useLocalStorage`, `useDebounce`, `useLocalStorage`).
Hooks should be generic and not tightly coupled to a single page's styling.
