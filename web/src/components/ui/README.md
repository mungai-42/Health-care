# UI Components

This folder contains atomic, reusable UI components (e.g., Button, Input, Select, Modal, Table).
These components must be style-agnostic or styled using variables defined in `src/styles/index.css`.
Avoid writing any business logic or HTTP calls inside these components.
